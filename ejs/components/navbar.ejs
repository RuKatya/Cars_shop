<nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Home</a>
        <div class="navbar-nav">

            <!-- Cars page -->
            <% console.log(title) %>
                <% if (title=="Cars" ) { %>
                    <a class="nav-link active" href="/cars">Cars</a>
                    <% } else { %>
                        <a class="nav-link" href="/cars">Cars</a>
                        <% } %>

                            <!-- Add car -->
                            <% if (title=="Add car" ) { %>
                                <a class="nav-link active" href="/addcar">Add new car</a>
                                <% } else { %>
                                    <a class="nav-link" href="/addcar">Add new car</a>
                                    <% } %>

                                        <!-- Orders -->
                                        <% if (title=="Orders" ) { %>
                                            <a class="nav-link active" href="/orders">Orders</a>
                                            <% } else { %>
                                                <a class="nav-link" href="/orders">Orders</a>
                                                <% } %>

                                                    <!-- Auth -->
                                                    <% if (title=="Authorization" ) { %>
                                                        <a class="nav-link active" href="/auth/login"><i
                                                                class="bi bi-person"></i></a>
                                                        <% } else { %>
                                                            <a class="nav-link" href="/auth/login"><i
                                                                    class="bi bi-person"></i></a>
                                                            <% } %>

                                                                <!-- Cart -->
                                                                <% if (title=="Cart" ) { %>
                                                                    <a class="nav-link active" href="/cart"><i
                                                                            class="bi bi-cart"></i></a>
                                                                    <% } else { %>
                                                                        <a class="nav-link" href="/cart"><i
                                                                                class="bi bi-cart"></i></a>
                                                                        <% } %>

        </div>
    </div>
</nav>